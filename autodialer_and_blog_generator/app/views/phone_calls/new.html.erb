<h1><i class="fas fa-phone-plus"></i> Make New Calls</h1>

<div class="row">
  <div class="col-md-8 mx-auto">
    <div class="card">
      <div class="card-body">
        <%= form_with url: phone_calls_path, method: :post, local: true, multipart: true do |f| %>
          
          <!-- Phone Numbers Input -->
          <div class="mb-4">
            <label class="form-label"><strong>Enter Phone Numbers</strong></label>
            <p class="text-muted small">Enter one phone number per line, or comma-separated. Use 1-800 numbers for testing!</p>
            <%= f.text_area :phone_numbers, class: "form-control", rows: 10, placeholder: "+18001234567\n+18005551234\n+18009876543" %>
          </div>

          <!-- File Upload -->
          <div class="mb-4">
            <label class="form-label"><strong>Or Upload a File</strong></label>
            <p class="text-muted small">Upload a text file with phone numbers (one per line)</p>
            <%= f.file_field :file, class: "form-control", accept: ".txt,.csv" %>
          </div>

          <!-- Custom Message -->
          <div class="mb-4">
            <label class="form-label"><strong>Custom Message (Optional)</strong></label>
            <p class="text-muted small">Leave blank for default greeting</p>
            <%= f.text_area :message, class: "form-control", rows: 3, placeholder: "Hello! This is a test call from Autodialer..." %>
          </div>

          <!-- Submit Buttons -->
          <div class="d-grid gap-2">
            <%= f.submit "Start Auto-Dialing", class: "btn btn-primary btn-lg", data: { confirm: "Are you sure you want to start calling these numbers?" } %>
            <%= link_to "Cancel", phone_calls_path, class: "btn btn-outline-secondary" %>
          </div>
        <% end %>
      </div>
    </div>

    <!-- Testing Info -->
    <div class="alert alert-warning mt-4">
      <h5><i class="fas fa-exclamation-triangle"></i> Important Testing Information</h5>
      <p class="mb-0">
        <strong>For testing, only verified numbers in Twilio account will work.</strong><br>
        I have only verified my number`+918433673635` in Twilio, so only calls to that number will go through successfully.<br>
      </p>
    </div>
  </div>
</div>
